<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.song.com.jd_demo">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/jd"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyAct" />
        <activity android:name=".ErWeiMa" />
        <activity android:name=".fragment.ZhuCeAct"/>
        <activity
            android:name=".GGactivity"
            android:label="@string/title_activity_ggactivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>